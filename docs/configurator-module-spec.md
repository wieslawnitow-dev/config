# ТЗ: универсальный конфигуратор кастомных изделий

## 1. Назначение

Модуль конфигуратора предназначен для сайтов-лидогенераторов, которые продают изделия, изготавливаемые по индивидуальным параметрам: площадь, периметр, погонные метры, количество, материалы, механизмы, монтаж и дополнительные услуги.

Первый тестовый продукт: москитные сетки.

Будущие продуктовые ниши:

- москитные сетки;
- жалюзи;
- ролеты;
- ворота;
- маркизы;
- плиссе;
- другие изделия на заказ с расчетом по размерам.

## 2. Общая архитектура

Стек:

- Astro SSG для статических SEO-страниц;
- React island для интерактивного конфигуратора;
- конфиги продуктов, стран, городов, цен и правил совместимости хранятся отдельно от компонента;
- расчет выполняется в браузере пользователя;
- серверная логика для расчета цены не требуется.

Конфигуратор должен работать на обычном статическом хостинге после сборки Astro.

## 3. Размещение на страницах

Конфигуратор размещается на коммерческих страницах:

- главная;
- страницы цены;
- страницы видов изделий;
- страницы объектов установки;
- страницы монтажа;
- городские страницы;
- mono-location и multi-location версии сайтов.

Отдельная страница калькулятора не обязательна. Если создается, она может быть служебной и не индексироваться.

## 4. Performance-модель

Цель: не ухудшать скорость Astro-страниц.

Требования:

- страница должна загружаться как обычная статическая Astro-страница;
- конфигуратор должен быть виден пользователю как статический HTML shell;
- React-логика не должна запускаться при первичной загрузке страницы;
- полная интерактивность включается после первого действия пользователя в блоке конфигуратора;
- shell и React-версия должны иметь одинаковую HTML/CSS-геометрию, чтобы избежать CLS;
- место под превью, цену, подсказки и ошибки должно быть зарезервировано заранее;
- нельзя использовать `client:load` для тяжелого конфигуратора на всех коммерческих страницах;
- предпочтительная модель: static shell + lazy/dynamic activation on first interaction.

## 5. Контекст страницы

Контекст задается внутри страницы, а не через URL-параметры.

Примеры контекста:

```ts
pageContext = {
  product: 'mosquito_nets',
  city: 'kyiv',
  defaultObject: 'balcony_door',
  defaultType: 'dvernye',
  defaultMesh: 'antikoshka',
  defaultService: 'turnkey'
}
```

Контекст страницы должен:

- предзаполнять конфигуратор;
- ограничивать доступные варианты;
- менять подсказки;
- менять стартовое превью;
- менять расчет монтажа/доставки/замера;
- не менять SEO-теги после загрузки страницы.

## 6. Модель заказа

Заказ может содержать несколько разных изделий одновременно.

```text
Order
├─ country
├─ city
├─ currency
├─ service_mode
├─ urgency
├─ items[]
└─ customer
```

Пример:

```text
Позиция 1: окно, рамочная сетка, 800x1200, 3 шт
Позиция 2: балконная дверь, дверная сетка, 700x2100, 1 шт
Позиция 3: окно, антикошка, 900x1300, 1 шт
```

## 7. Модель позиции

```text
Item
├─ object_type
├─ product_type
├─ width
├─ height
├─ quantity
├─ profile_color
├─ fabric_or_mesh_type
├─ mounting_type
├─ mechanism
├─ automation
└─ optional_addons
```

Минимальный набор для расчета:

- объект установки;
- тип изделия;
- ширина;
- высота;
- количество;
- цвет профиля/рамки;
- тип полотна/материала;
- сервисный сценарий.

Расширенный набор:

- крепление;
- механизм;
- автоматика;
- срочность;
- доставка;
- замер;
- монтаж;
- демонтаж;
- комментарий;
- фото объекта.

Фото объекта не участвует в расчете на первом этапе, но может передаваться в заявку.

## 8. Геометрия и расчетные модели

Модуль должен поддерживать несколько измерительных моделей.

```text
area_m2 = width_m * height_m
perimeter_m = 2 * (width_m + height_m)
linear_m = length_m * quantity
unit = fixed count
```

Применение:

- `area_m2`: полотно, сетка, ламели, панели;
- `perimeter_m`: профиль, рамка, покраска, уплотнители;
- `linear_m`: направляющие, рельсы, карнизы, профили;
- `unit`: фурнитура, крепления, ручки, моторы, пульты;
- `service`: замер, доставка, монтаж, демонтаж, сложность объекта.

## 9. Формула цены

Формула должна быть модульной, а не зашитой под один продукт.

Базовая логика позиции:

```text
item_price =
  area_m2 * material_price_m2
  + perimeter_m * profile_price_m
  + perimeter_m * paint_price_m
  + linear_m * linear_component_price_m
  + unit_components_price
  + mechanism_price
  + automation_price
  + mounting_hardware_price
```

После этого применяются:

```text
position_price = max(item_price, min_item_price) * quantity
```

Сервисная часть:

```text
service_price =
  measure_price
  + delivery_price
  + installation_base
  + installation_by_product_type
  + installation_by_object_type
  + installation_by_complexity
  + dismantling_price
  + urgency_modifier
```

Итог:

```text
total = sum(position_price) + service_price
```

## 10. Отключаемые и фиксированные параметры

Каждое поле должно поддерживать режимы:

```text
selectable
fixed
disabled
```

`selectable`:

- пользователь видит поле;
- пользователь выбирает значение;
- значение участвует в расчете.

`fixed`:

- поле может быть скрыто или показано как фиксированное;
- значение задано конфигом;
- значение участвует в расчете.

`disabled`:

- поле скрыто;
- значение не участвует в расчете;
- параметр выбрасывается из формулы.

## 11. Совместимость опций

Форма должна показывать только доступные комбинации.

Пример:

```text
mesh = antikoshka
→ доступны только типы изделий, которые поддерживают антикошку
```

```text
type = plisse
→ antikoshka скрыта, если плиссе не поддерживает такое полотно
```

Фильтрация должна работать в обе стороны:

- выбранное полотно фильтрует типы изделий;
- выбранный тип изделия фильтрует полотна;
- выбранный объект фильтрует типы изделий;
- выбранный механизм фильтрует автоматику;
- выбранный сервис фильтрует доступные условия монтажа/доставки.

Рекомендуемая модель:

```text
selected constraints
→ find valid configurations
→ show only option values present in valid configurations
```

Если текущий выбор становится несовместимым, система должна:

- не ломаться;
- заменить значение на ближайшее допустимое;
- показать короткую подсказку пользователю.

## 12. Условная видимость полей

Поля показываются только если они релевантны текущей конфигурации.

Примеры:

```text
рамочная сетка на окно
→ automation скрыта
```

```text
рулонная сетка на террасу
→ automation доступна
```

```text
откатные ворота
→ automation доступна
→ construction_work доступна
```

Логика:

```text
field.visible = selected_configuration.supports(field)
```

## 13. Автоматика и управление

Автоматика должна быть универсальным модулем, не привязанным только к воротам.

```text
automation
├─ none
├─ manual
├─ motorized
├─ remote_control
├─ smart_home
├─ sensor_based
└─ timer_based
```

Применение:

- москитки: моторизованные рулонные системы, пульт, smart home;
- жалюзи/ролеты: электропривод, датчики солнца/ветра, smart home;
- ворота: привод, пульты, фотоэлементы, GSM/Wi-Fi, smart home.

## 14. Дополнительные модули для ворот

Если ворота планируются как важная ниша, заложить опциональные модули сразу.

```text
opening_type
```

- секционные;
- откатные;
- распашные;
- рулонные;
- гаражные;
- промышленные.

```text
construction_work
```

- подготовка проема;
- бетонирование;
- столбы;
- направляющая;
- демонтаж старых ворот.

```text
access_control
```

- пульт;
- клавиатура;
- фотоэлементы;
- датчики;
- домофон;
- GSM/Wi-Fi модуль.

```text
site_conditions
```

- ширина/высота проема;
- уклон;
- тип основания;
- наличие электричества;
- сложность подъезда.

## 15. Визуальное превью

Конфигуратор должен показывать небольшое, четкое, недетализированное превью изделия.

Превью должно меняться по параметрам:

```text
object_type
product_type
profile_color
fabric_or_mesh_type
mounting_type
mechanism
automation
```

Для первого этапа достаточно схематического CSS/SVG-превью без тяжелых изображений.

Требования:

- превью не должно ухудшать LCP;
- размеры области превью фиксированы;
- изменение превью не должно вызывать layout shift;
- превью должно передавать интент конфигурации, а не быть фотореалистичным.

## 16. UX-компоновка

Wizard по шагам не использовать как основной интерфейс.

Форма должна быть открытой: пользователь видит параметры, превью и цену.

Mobile:

```text
┌─────────────────────────┐
│ Превью + цена           │
├─────────────────────────┤
│ Объект установки        │
│ Тип изделия             │
│ Размеры                 │
│ Цвет                    │
│ Полотно/материал        │
│ Крепление/механизм      │
│ Сервис                  │
├─────────────────────────┤
│ + Добавить изделие      │
│ Итог заказа             │
│ Контакты                │
└─────────────────────────┘
```

Desktop:

```text
┌──────────────────────┬──────────────────────┐
│ Параметры изделия    │ Превью + расчет       │
│                      │                      │
│ Объект               │ Превью               │
│ Тип                  │ Цена позиции          │
│ Размеры              │ Итог заказа           │
│ Цвет                 │ CTA                   │
│ Материал             │                      │
│ Крепление            │                      │
│ Сервис               │                      │
└──────────────────────┴──────────────────────┘
```

## 17. Заявка

Форма заявки должна передавать:

- сайт;
- страна;
- город;
- страница отправки;
- контекст страницы;
- все позиции заказа;
- рассчитанная цена;
- выбранный сервис;
- имя;
- телефон;
- мессенджер;
- комментарий;
- прикрепленные фото, если есть.

Интеграция отправки должна быть заменяемой:

- PHP endpoint на хостинге;
- внешний form backend;
- webhook CRM;
- email gateway;
- Telegram gateway.

## 18. Аналитика

События:

```text
configurator_view
configurator_activate
item_add
item_remove
option_change
price_calculated
lead_submit
phone_click
messenger_click
```

Параметры событий:

```text
product
city
country
page_type
object_type
product_type
service_mode
estimated_total
items_count
```

## 19. SEO-ограничения

Конфигуратор не должен:

- менять title, description, canonical, robots после загрузки;
- создавать индексируемые URL-параметры;
- генерировать дубли страниц;
- блокировать чтение основного контента без JS;
- ухудшать LCP/CLS/INP коммерческих страниц.

Страница должна оставаться полноценной SEO-страницей без работающего JS.

## 20. Первый продукт: москитные сетки

Начальные типы изделий:

```text
ramochnye
rulonnye
plisse
dvernye
razdvizhnye
antikoshka
antipyl
```

Объекты установки:

```text
window
plastic_window
wooden_window
balcony
balcony_door
terrace
sliding_door
roof_window
```

Материалы/полотна:

```text
standard
antikoshka
antipyl
reinforced
better_view
```

Цвета профиля:

```text
white
brown
anthracite
grey
wood_like
ral_custom
```

Крепления/механизмы:

```text
standard_hooks
z_hooks
clips
hinges
magnet
guides
cassette
unknown
```

Сервисы:

```text
self_measure_delivery
measure_only
mount_only
turnkey
pickup
courier_delivery
```

## 21. Acceptance criteria

Модуль считается готовым для MVP, если:

- конфигуратор виден на статической Astro-странице до загрузки React;
- React активируется только после взаимодействия пользователя;
- можно добавить несколько изделий в один заказ;
- расчет учитывает площадь, периметр, погонные метры и штуки;
- цвет рамки может считаться через периметр и цену покраски за погонный метр;
- монтаж считается отдельно и зависит от изделия/объекта/сервиса;
- несовместимые параметры скрываются;
- фильтрация работает в обе стороны;
- параметры могут быть selectable/fixed/disabled;
- превью меняется от выбора пользователя;
- форма корректно работает на мобильном и десктопе;
- страница без JS остается читаемой и SEO-полезной;
- Lighthouse не показывает критичных просадок LCP/CLS/INP из-за модуля.
